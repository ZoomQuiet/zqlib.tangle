done * 安装配置脚本
done * 为文件标记tag
done * 显示某个tag下的所有文件(-l)
done * 显示用户所有tag(-a)
done * 删除指定tag，可处理多个(-d)
* 从一个tag中删除一个文件(-c) 已经实现，但有问题
* 删除所有tag: 可通过shell完成(使用xargs)
* 复合查找功能:同时含tag A和tag B的文件但不含tag C的文件：可通过shell实现(tagit -l A|grep `tagit-l B`|grep -v `tagit -l C`)
* 反向查询：查询某文件所被打上的所有tag
* 目录特殊处理
* 递归
* 软tag选项
* tag视图的shell
* 预设tag
* xapian重构板(C++)
